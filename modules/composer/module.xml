<?xml version="1.0" encoding="UTF-8"?>
<project name="composer">
	<!-- Mandatory targets -->
	<target name="configure" />
	<target name="build"  depends="update" />
	<target name="review" />
	<target name="migrate" />
	<target name="sync" />

	<!-- toolkit phase : build -->
	<target name="update" description="Updates dependencies managed by composer">
		<exec dir="${project.dirs.src}" executable="${composer.executable}" failOnError="true" resolveexecutable="true">
			<arg line="update" />
		</exec>
	</target>
</project>