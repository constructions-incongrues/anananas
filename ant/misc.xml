<?xml version="1.0" encoding="UTF-8"?>
<project name="misc">

	<target name="help" description="Displays project's help">
		<java classname="org.apache.tools.ant.Main">
			<arg value="-buildfile" />
			<arg value="${ant.file}" />
			<arg value="-projecthelp" />
		</java>
	</target>

	<target name="configure" description="Configure a project for an exploitation environment" depends="setprofile">
		<exec executable="${toolkit.basedir}/vendor/phpreprocessor/bin/phpreprocessor">
			<arg line="--command=apply
             --src=${source}
             --properties=${basedir}/etc/${profile}/build.properties" />
		</exec>
	</target>

	<!-- This target depends on several targets to make sure that one does not send a wrongly configured project to some env -->
	<target name="sync" description="(R)Sync your project files to a remote destination" depends="configure">
		<fail unless="profile" />
		<exec executable="rsync">
			<arg line="--cvs-exclude 
              --exclude-from=${basedir}/etc/${profile}/rsync.exclude
              --links
	      ${rsync.options}
              -agpvzc ${source}/ -e ssh ${remote.username}@${remote.host}:${remote.path.root}" />
		</exec>
	</target>
	
	<target name="deploy" description="Deploys latest changes to environment defined by profile" depends="liquibase.migrate,sync">
	</target>
	
	<target name="init" description="Inits empty project">
		<fail unless="profile" />
		<fail unless="toolkitDir" />

		<!-- Create directories -->
		<mkdir dir="${basedir}/src" />
		<mkdir dir="${basedir}/etc/liquibase/changelogs/" />
		
		<!-- Create team configuration profile -->
		<copy todir="${basedir}/etc/team">
			<fileset dir="${toolkitDir}/templates/profile/" includes="**/*" />
		</copy>
		<!-- Create local configuration profile -->
		<copy todir="${basedir}/etc/${profile}">
			<fileset dir="${toolkitDir}/templates/profile/" includes="**/*" />
		</copy>

		<!-- Backup existing build.properties file -->
		<copy file="${basedir}/etc/${profile}/build.properties" tofile="${basedir}/etc/${profile}/build.properties.bak" />

		<!-- Generate initial build.properties file from project sources -->
		<exec executable="${toolkitDir}/vendor/phpreprocessor/bin/phpreprocessor" output="${basedir}/etc/${profile}/build.properties" logError="true">
			<arg line="--command=extract --exclude-from=${toolkitDir}/etc/default/build.properties,${basedir}/etc/team/build.properties --src=${basedir}/src" />
		</exec>
		
		<!-- Create project build.xml file -->
		<copy todir="${basedir}" file="${toolkitDir}/templates/build.xml" />
	</target>
</project>
